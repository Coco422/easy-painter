# Easy Painter Roadmap

> 本路线图用于内部产品决策，不承诺固定发布日期。优先级会根据真实使用反馈、上游稳定性和维护成本调整。
>
> 最后审视：2026-08-05。当前仍以内部使用为主，但账务、任务和数据安全按未来开放注册的标准设计，避免用户规模扩大后再返工。

## 路线图怎么使用

- **P0（先保证可信）**：不完成就可能产生错误扣费、重复任务、数据丢失或无法定位生产故障。
- **P1（明显改善使用）**：能够提升创作效率、消费透明度、运营效率和系统稳定性。
- **P2（平台化发展）**：无限画布、团队协作、工作流和生态能力，需建立在 P0/P1 基础上。
- 每个候选进入开发前，应补充负责人、范围、数据迁移方案、回滚方案和可验证的验收标准。
- 本文记录的是问题与方向，不等同于全部立项；已完成能力归入基线，避免继续以待办形式重复维护。

## 当前能力基线

以下能力已经存在，后续规划应优先补齐闭环，不重复建设：

### 创作与资产

- 支持单次生成 1、2、4 张图片，模型、尺寸和宽高比可选。
- 支持点击、粘贴和拖拽预上传参考图，按时间倒序保留 50 张历史并可复用。
- 生成任务支持页面刷新后的自动恢复轮询，图片加载失败可重试。
- 我的画廊支持搜索、日期筛选、收藏、删除、公开、标签和提示词公开控制。
- 公开画廊可由游客访问；社区灵感来自本站用户主动公开的内容，灵感库来自互联网公开提示词。

### 账号与通知

- 支持邮箱验证码注册、用户名或邮箱登录、忘记密码、登录后绑定邮箱。
- 邮件验证码已具备邮箱、IP、用户维度的发送冷却、次数限制和错误次数限制。
- 支持面向全部访客、登录用户和未绑定邮箱用户的多条系统横幅通知。

### 消费与管理

- 模型支持启停、拖拽排序、单张灵感丝线成本和支持尺寸配置。
- 支持兑换码批量生成、单条/批量复制、用户余额人工调整和流水查看。
- Admin 已覆盖总览、上游、模型、用户、任务、计费和通知管理。

### 任务与账务可靠性

- 数据库使用 Flyway V1–V3 forward-only migrations 管理，并提供独立 migrations 镜像；迁移失败时业务服务不会启动。
- 每张生成任务使用稳定 `Idempotency-Key` 和请求摘要；任务、消费预扣、负流水与 outbox 在同一数据库事务内创建。
- 余额扣减采用非负条件原子更新，消费按 `reserved -> settled/refunded` 流转，失败任务统一全额退款。
- `credit_transactions` 为 append-only 结构化流水，保留任务、原流水和余额快照关联；Dispatcher 会自动对账并修复余额缓存。
- Transactional outbox、常驻 Dispatcher 和 Worker 幂等领取共同处理消息投递、重复消息和进程中断。
- Watchdog 会收敛排队或执行超时任务；迟到 Worker 不得重新结算或公开已退款任务的图片。
- 任务保存模型、渠道和价格快照；Admin 可查看账务状态、退款、投递、执行状态和依赖健康。
- 创作台在提交前展示预计消耗，批量生成允许部分成功，并展示实际扣费和全额退款结果。

### 发布与部署

- 使用根目录 `VERSION`、中文 `CHANGELOG.md` 和标准 `vX.Y.Z` 语义化版本。
- 前端版本中心可展示内置日志并检查 GitHub 最新正式 Release。
- GitHub Actions 可构建 API、Nginx 和 migrations 三类 GHCR 镜像，并由 tag 工作流从 changelog 创建 Release。
- `v0.12.0` 已作为首个包含完整账务可靠性基线的正式版本发布。

## 下一阶段优先补齐（P0）

| 优先项 | 目标 | 最小验收 |
| --- | --- | --- |
| 备份与恢复演练 | 为 PostgreSQL 与 MinIO 建立自动备份、保留、加密和一致性恢复方案 | 在隔离环境完成恢复，记录 RPO/RTO，并抽样核对账号、余额、任务与图片对象 |
| 安全重试与取消 | 管理员可重试或取消异常任务，用户可取消尚未开始的任务 | 所有操作遵守任务状态机和账务幂等，取消或最终失败只退款一次 |
| 外部告警 | 将 Worker 离线、队列积压、失败率、对账异常和 SMTP 故障主动发送到外部渠道 | 告警具备去重、恢复通知和脱敏信息，不依赖管理员持续刷新后台 |
| 真实成本与利润视角 | 区分用户支付的灵感丝线与上游真实成本、汇率、补贴和失败成本 | 可按模型、渠道和时间聚合成功图片成本及毛成本，历史记录不受后续调价影响 |
| 管理权限与审计 | 用管理员账号和 RBAC 替代共享密钥，并记录敏感操作 | 至少区分只读、运营、客服和系统管理员；调账、重置密码、密钥与批量操作可追溯 |
| 容量保护与渠道降级 | 在渠道异常或系统满载时限制新任务，并按健康状态路由或熔断 | 支持用户/全局并发上限、队列上限、渠道熔断恢复和明确的用户提示 |

这些事项涉及持久化结构、任务状态或生产运维，进入开发前必须补充迁移、回滚、故障注入和真实 PostgreSQL 并发测试方案。

## P1：创作体验与资产效率

### 更容易做出想要的结果

- [ ] **模型选择助手**：按“写实、人像、海报、插画、参考图、文字生成”等能力标签推荐模型，展示预计耗时、价格和稳定性。
- [ ] **提示词工作台**：提供变量模板、常用片段、负面提示词、历史版本、对比和一键恢复；提示词增强必须允许查看并保留原文。
- [ ] **参数预设**：保存模型、尺寸、张数和提示词组合，支持个人预设与管理员推荐预设。
- [ ] **生成后连续动作**：复用参数、生成变体、放大、局部重绘、扩图、去背景和继续编辑；按上游能力动态显示，不展示不可用按钮。
- [ ] **结果对比**：并排比较多张图，保留参数和成本，快速选出最终版本。
- [ ] **可复现参数**：上游支持时记录 seed、质量和风格参数；复制任务时明确哪些参数可复现、哪些由渠道随机决定。
- [ ] **可预期的等待**：展示队列位置、粗略 ETA、已等待时间、重试次数和安全取消入口。
- [ ] **完成通知**：长任务在用户离开页面后可通过站内通知或用户自愿开启的邮件提醒完成/失败，不默认发送营销邮件。
- [ ] **草稿保护**：提示词、参数和当前参考图在刷新、误关页面或登录失效后可恢复。
- [ ] **新手首张图**：用少量高质量示例、推荐模型和安全默认参数缩短从注册到首次成功生成的路径。
- [ ] **快捷操作与无障碍**：补充键盘快捷键、焦点管理、屏幕阅读提示、触控命中区和弱网体验。

### 资产不再散落

- [ ] **项目与文件夹**：按项目组织提示词、参考图、任务和成图，而不是只有时间倒序画廊。
- [ ] **统一素材库**：把参考图历史升级为可搜索、收藏、标签、重命名和批量管理的素材库。
- [ ] **智能检索**：按提示词、模型、时间、尺寸、标签、收藏、公开状态和生成结果搜索。
- [ ] **批量操作**：批量收藏、打标签、移动、公开、下载和删除；危险操作支持回收站与恢复期限。
- [ ] **元数据导出**：下载图片时可选择同时导出提示词、模型、参数、任务 ID 和版本信息。
- [ ] **存储生命周期**：允许管理员配置原图、参考图、失败任务和回收站的保留策略，并提前告知用户。

## P1：消费系统与内部运营

### 对用户更可控

- [ ] **预算与提醒**：支持日/月消费上限、单次高额确认、低余额提醒和异常消费提醒。
- [ ] **快速/慢速队列**：参考成熟创作工具的 Fast/Relax 思路，让高峰期可在“更快但更贵”和“排队但更省”之间选择。
- [ ] **额度账户分层**：区分赠送、兑换、月度和购买额度，定义有效期、消耗顺序、退款回原账户规则。
- [ ] **消费报表**：个人中心展示按日、模型、项目的用量趋势及失败退款，不以“成瘾式排行榜”刺激无意义消费。
- [ ] **价格变更提示**：模型价格变更时显示生效时间；收藏的预设若成本变化，在提交前二次确认。

### 对内部更可运营

- [ ] **模型定价矩阵**：支持模型 × 渠道 × 尺寸 × 张数 × 队列等级，而不是只有统一“丝/张”。
- [ ] **兑换码生命周期**：增加批次、用途、有效期、领取对象、使用上限、禁用、导出和核销统计。
- [ ] **用户组与部门预算**：为内部团队分配额度、月度预算和管理员，支持成本中心汇总。
- [ ] **异常消费检测**：识别短时高频生成、共享账号、脚本滥用、同设备多账号和上游成本突增。
- [ ] **运营分群**：按活跃、留存、模型偏好、失败率、邮箱绑定和余额区间筛选用户，用于通知和支持，不默认用于营销追踪。

> 内部阶段仍不建议优先接入在线支付。应先完成真实成本核算、权限审计和备份恢复，并确认存在稳定购买需求，再评估订单、发票、支付回调及财务合规。

## P1：稳定性、安全与后台能力

### 渠道与任务可靠性

- [ ] **受控重试**：按错误类型决定是否重试、退避时间和最大次数；避免不可重试错误继续消耗上游成本。
- [ ] **可观测链路**：用 request ID / job ID 串联 API、队列、Worker、上游和存储日志。
- [ ] **维护模式与状态页**：维护或核心依赖异常时阻止新任务进入，保留历史查看并向用户说明影响范围。
- [ ] **合成探测**：定时执行不产生真实高额成本的最小链路检查，区分“接口能连接”和“确实能完成生成”。

### 管理与审计

- [ ] **密钥治理**：上游、SMTP 和管理员凭证支持轮换、最小权限、脱敏展示和最后使用时间。
- [ ] **会话安全**：支持撤销全部会话、登录设备查看、异常登录提醒和管理员强制下线。
- [ ] **限流统一化**：把注册、登录、验证码、生成、公开互动和下载统一纳入可配置限流及审计。

### 支持与数据治理

- [ ] **用户支持视图**：在不暴露密码和完整密钥的前提下查看用户任务、流水、退款和邮箱状态。
- [ ] **诊断包**：管理员可导出脱敏后的版本、依赖健康、队列和近期错误摘要，方便开源部署者排障。
- [ ] **数据导出与注销**：明确用户数据导出、账号注销、公开作品撤回和对象清理流程。
- [ ] **隐私默认值**：新任务默认私有；公开作品前明确展示图片、提示词、用户名和标签的公开范围。
- [ ] **内容举报与审核**：公开社区增加举报、隐藏、审核状态、敏感内容分级和申诉记录。
- [ ] **邮件可投递性**：记录发送结果、退信和供应商错误，配置 SPF/DKIM/DMARC 检查提示，但不保存验证码明文。

## P2：无限画布与工作流平台

### 无限画布

- [ ] 调研并制作 [basketikun/infinite-canvas](https://github.com/basketikun/infinite-canvas) 集成 PoC，验证节点模型、图片资产、缩放平移和生成任务编排能力。
- [ ] 在正式集成前完成许可证评估。该项目当前采用 `AGPL-3.0`；如果与 Easy Painter 最终许可证或商业使用边界不兼容，则不直接复用其代码。
- [ ] 根据 PoC 结果在“合规集成现有项目”和“自研轻量无限画布”之间做技术决策。
- [ ] 将提示词、参考图、生成任务、图像处理和输出抽象为可连接节点。
- [ ] 支持画布持久化、撤销重做、版本快照、导入导出和大画布性能优化。
- [ ] 首版先服务单人可视化创作，不一开始就同时承担多人白板、复杂排版和完整图像编辑器职责。

### 可复用工作流

- [ ] 从一张成功作品回溯完整参数链路，并一键复制为新工作流。
- [ ] 工作流节点支持输入校验、成本预估、运行前确认、局部重跑和缓存复用。
- [ ] 提供只读分享、模板发布、版本锁定和依赖检查，避免模板随模型下线后静默失效。
- [ ] 支持 API、Webhook 和计划任务前，先完成权限、幂等、配额和审计能力。
- [ ] 评估插件机制、自定义节点和第三方生成渠道适配层；插件必须隔离密钥与执行权限。

### 团队协作

- [ ] 项目共享、成员角色、评论、批注、版本历史和交付确认。
- [ ] 团队素材库、品牌风格、受控提示词模板和部门预算。
- [ ] 明确个人资产、团队资产和公开社区资产的所有权、退出团队后的保留规则。

## P2：社区、增长与模型资产

- [ ] **复用链路**：公开作品可一键复用提示词和参数，并保留作者归因与来源链接。
- [ ] **内容策展**：精选专题、合集、编辑推荐和高质量模板，排序不只依赖点赞数。
- [ ] **反作弊互动**：点赞、收藏和榜单具备限流、异常检测和管理员修正能力。
- [ ] **创作者激励**：未来可评估贡献奖励，但必须与消费账本分离，避免“奖励币”和付费额度语义混乱。
- [ ] **风格资产**：支持经过授权的 LoRA、风格参考和品牌资产；记录许可证、来源、可用模型和使用范围。
- [ ] **内容来源标记**：评估水印、生成元数据或 C2PA 等来源证明，公开作品明确区分 AI 生成、用户上传和二次编辑。
- [ ] **模型评测**：用固定测试集比较画质、提示词遵循、中文文字、参考图一致性、耗时、失败率和成本。
- [ ] **开放 API**：为可信内部调用方提供 API key、配额、审计、幂等和 Webhook；不直接复用浏览器登录令牌。

## 开源分发与更新演进

- [x] 建立 `vX.Y.Z` 语义化版本、根目录 `VERSION` 和人工维护的中文 `CHANGELOG.md`。
- [x] 在前端展示构建时内置的版本时间线，并在打开版本中心时检查 GitHub 最新正式 Release。
- [x] tag 发布工作流校验版本一致性，并从 changelog 对应章节生成 GitHub Release。
- [ ] 区分仅前端更新、整仓更新和需要数据迁移的版本，避免前后端版本不一致。
- [ ] 发布产物附带构建来源、commit SHA、镜像摘要和可验证签名/SBOM。
- [ ] 支持升级前下载并校验新版本部署包，但仍由管理员确认后同时更新前后端。
- [ ] 提供升级前检查、配置备份、数据库迁移提示、版本兼容矩阵和可回滚部署流程。
- [ ] 允许第三方部署者配置更新仓库、更新通道或关闭远端检查，同时保留官方仓库的安全默认值；不实现应用内静默自更新。

## 成熟产品机制参考

以下参考只用于理解已经被验证的产品机制，不照搬套餐名称、实时价格或商业策略：

| 参考 | 可借鉴机制 | 对 Easy Painter 的启发 |
| --- | --- | --- |
| [Midjourney Plans](https://docs.midjourney.com/hc/en-us/articles/27870484040333-Comparing-Midjourney-Plans) | Fast / Relax 分层、并发任务与排队任务限制、Stealth 私密模式 | 队列等级、容量保护、明确的公开/私密边界 |
| [Runway Pricing](https://runway.com/pricing) | Credits 作为跨模型统一消费单位，不同模型/时长/分辨率消耗不同 | 统一灵感丝线体验，但用价格快照解释差异化成本 |
| [Stripe Idempotent Requests](https://docs.stripe.com/api/idempotent_requests) | POST 请求通过幂等键安全重试 | 创建任务和扣费必须绑定同一用户意图，避免重复执行 |
| [Stripe Customer Balance Transactions](https://docs.stripe.com/api/customer_balance_transactions) | 余额变化使用明确类型和 ending balance 的交易记录 | 建立只追加、可关联、可重算的灵感丝线流水 |

借鉴这些机制时遵守三条原则：

1. **先保证正确，再设计促销**：复杂套餐和支付能力必须复用现有幂等、退款和对账闭环，不能另建旁路账本。
2. **先解释消费，再引导消费**：用户必须在提交前知道成本，在失败后知道退款结果。
3. **先验证真实瓶颈，再平台化**：无限画布、插件和工作流建立在稳定任务、资产与账务模型之上。

## 建议建立的产品指标

指标用于发现问题和验证改进，不用单一“生成量”驱动产品：

### 用户体验

- 首次成功生成率、从注册到首张成功图片的耗时。
- 生成提交成功率、最终成功率、取消率和失败退款完成时间。
- P50/P95 排队时间、生成时间、图片可见时间。
- 参数复用率、参考图复用率、收藏/下载/公开率。
- 因余额不足中断的请求比例、低余额提示后的有效操作。

### 消费与成本

- 每日发放、兑换、预扣、结算、退款、人工调账和过期额度。
- 用户余额与流水重算余额不一致数量，应长期为 0。
- 按模型/渠道/尺寸的用户消费、真实成本、失败成本和单位成功图片成本。
- 重复请求被幂等命中的次数，以及因此避免的重复扣费金额。

### 稳定性与运营

- API、Worker、队列、MinIO、SMTP 和各上游的可用率。
- 按错误类型统计失败率、重试成功率、卡死任务数量和平均恢复时间。
- 队列深度、Worker 心跳延迟、单用户并发与全局容量。
- 管理员人工补账、手动重试和用户支持请求数量，并持续观察其下降趋势。

## 暂不优先做

- 在真实成本、权限审计和备份恢复尚未就绪时接入在线支付、订阅、自动续费、发票或复杂促销。
- 在单人创作资产尚未组织好前，直接开发重型多人实时协作。
- 为追求模型数量接入大量不稳定渠道，而没有健康检查、成本快照和熔断。
- 允许应用自动替换前后端文件或静默执行数据库迁移。
- 用不可解释的算法刺激用户持续消费，或默认把用户作品、提示词公开。
- 在许可证、数据来源和内容审核边界不清楚时直接引入第三方模型、风格包或社区内容。
